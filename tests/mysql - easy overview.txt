mysql -u root

use eiou

SELECT message FROM debug;

SELECT id, address, name, status, fee_percent, credit_limit, currency FROM contacts;

SELECT id, hash, amount, my_fee_amount, sender_address, destination_address, incoming_txid, outgoing_txid, status, created_at, completed_at FROM p2p;

SELECT id, hash, time, amount, sender_address FROM rp2p;

SELECT id, status, tx_type, sender_address, receiver_address, amount, currency, timestamp, txid, previous_txid, memo FROM transactions;