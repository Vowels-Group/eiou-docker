node_modules
yarn.lock
.swarm/*
.hive-mind/*
.claude-flow/*
# Ignore generated docs but track API documentation
docs/*
!docs/*.md
memory/*

# Test credentials (credentials.example.sh should be committed as template)
tests/baseconfig/credentials.sh

# Any local credential files
*.credentials
*.secrets
.env.local
.env.test.local

# PHPUnit cache and local config
tests/.phpunit.cache/
tests/phpunit.xml

# Composer dependencies (in files/ directory)
# composer.lock is tracked — ensures deterministic Docker builds
files/vendor/

# OS / editor artifacts
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Sensitive files
.env
*.pem
*.key
*.crt
*.p12

# Logs and databases
*.log
*.db
*.sqlite
